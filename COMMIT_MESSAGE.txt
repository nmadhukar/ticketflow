feat: add stats dashboard, S3 monitoring, enhanced S3 service, and fix TaskModal re-rendering

Major Features:
- Add stats dashboard for agents and managers with personal/team metrics
  * Agent stats: personal ticket stats and team overview with dropdown
  * Manager stats: department overview, priority distribution, category breakdown, team performance
  * Real-time updates via WebSocket integration
  * Responsive drawer component with left-side opening
- Add S3 usage monitoring to admin dashboard
  * Total storage and file count metrics
  * Daily and monthly usage trends with charts
  * Recent uploads table with task links
  * Warning about potential discrepancies from deleted files
- Enhance S3 service with advanced operations
  * Add fileExists() for file verification
  * Add getFileMetadata() for retrieving file metadata
  * Add listObjects() with prefix filtering and pagination
  * Add getStorageStats() for calculating storage by prefix
  * Add deleteFiles() for batch deletion operations
  * Add healthCheck() for S3 connection verification
  * Add getRegion() and getBucketName() getters

Bug Fixes:
- Fix TaskModal infinite re-rendering issue
  * Add useRef to track last processed task ID
  * Memoize teams array to prevent unnecessary re-renders
  * Add guard to prevent re-processing same task
  * Fix dependency array in useEffect to break render loop

Improvements:
- Refactor server routes from single file to modular structure
- Improve team member access control for managers
  * Restrict "Add Member" action to team creators (unless admin)
  * Fix team member visibility for agents and managers
- Uncomment and fix /api/teams/my endpoint for proper team filtering
- Add explicit queryFn to team-related queries for consistency
- Update WebSocket handlers to invalidate stats queries
- Add translations for stats dashboard (en, es, fr, de, zh)

Technical Changes:
- Add getAgentStats, getManagerStats, getS3UsageStats to storage interface
- Implement stats aggregation queries with proper joins and grouping
- Create reusable StatCard component with proper text truncation
- Add stats type definitions and custom hooks (useStats)
- Update task mutations to invalidate stats queries
- Improve error handling in team detail page
- Enhance S3 service with ListObjectsV2Command for better object management

Files Changed:
- New: stats components, hooks, types, translations
- New: enhanced S3 service methods
- Modified: TaskModal, team-detail, teams, dashboard, WebSocket hooks
- Refactored: server routes structure
